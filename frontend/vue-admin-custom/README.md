# Vue Admin Custom

Lightweight Vue 3 + Vite admin shell with Element Plus.

Quick start:

```bash
cd frontend/vue-admin-custom
npm install
npm run dev
```

Features:
- Theme color set to Element default
- Left collapsible sidebar with module entries (uses Element icons)
- `TradingDashboard` sample page
- Placeholder pages to embed external modules via iframe later
