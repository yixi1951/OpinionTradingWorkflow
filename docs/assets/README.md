# Dashboard screenshots

Place marketing / README images here:

- `dashboard-hero.png` — main Streamlit view (Hero KPIs, tabs)
- Optional: `eval-walk-forward.png`, `analyst-consensus.png`

Capture after `streamlit run src/opinion_trading/ui_dashboard.py` with sample data under `data/`.